import { StationConfig } from "@common/config/config";
import { RailSignal } from "@common/nodes/signal";

export const allowSignal = (stationConfig: StationConfig, signal: RailSignal, target: number, aspect: number = -1) => {
    
}
